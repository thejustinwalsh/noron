{
	"name": "@noron/bench-exec",
	"version": "0.3.1",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "bun build ./src/main.ts --compile --outfile dist/bench-exec --target ${BUN_TARGET:-bun-linux-x64}",
		"typecheck": "tsc --noEmit",
		"test": "bun test"
	},
	"dependencies": {
		"@noron/shared": "workspace:*"
	}
}
