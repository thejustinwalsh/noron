{
	"name": "@noron/web",
	"version": "0.3.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "bun --watch src/main.ts",
		"build": "bun build ./src/main.ts --compile --outfile dist/bench-web --target ${BUN_TARGET:-bun-linux-x64}",
		"typecheck": "tsc --noEmit",
		"test": "bun test"
	},
	"dependencies": {
		"@noron/shared": "workspace:*",
		"@zod/mini": "^4.0.0-beta.0",
		"hono": "^4",
		"openworkflow": "^0.8.0"
	}
}
