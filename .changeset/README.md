# Changesets

This project uses [changesets](https://github.com/changesets/changesets) for version management.

Changesets are auto-generated from conventional commits on PRs. You can also create one manually:

```bash
bun changeset
```
